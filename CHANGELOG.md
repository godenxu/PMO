# PMO 棱镜驾驶舱 (PMO Prism Cockpit) - 项目架构与修改日志

> **项目说明**：本项目为基于 Three.js + 原生 WebGL 构建的 3D 全景三维项目管理与遥测总控驾驶舱，单文件纯前端架构（直接浏览器双击 `index.html` 即可离线流畅运行，无需任何后台或构建工具）。

---

## 目录
1. [核心架构与视觉特性](#1-核心架构与视觉特性)
2. [双大屏与视线交互系统](#2-双大屏与视线交互系统)
3. [操控手感与物理学算法](#3-操控手感与物理学算法)
4. [移动端深度缓冲与抗闪烁优化](#4-移动端深度缓冲与抗闪烁优化)
5. [性能瓶颈与高帧率极致优化](#5-性能瓶颈与高帧率极致优化)
6. [近期重要优化与调优记录](#6-近期重要优化与调优记录)

---

## 1. 核心架构与视觉特性

- **26 系统水晶棱镜蜂巢矩阵 (Honeycomb Crystal Cubes)**：
  - **通透能量水晶质感**：高折射高镜面光斑（`shininess: 240`）与高纯度发光基底，通体清澈通透；
  - **双层锐利冷白激光倒角高光**：在方块 12 条棱线上叠加了微距外扩（`1.004`）的纯净冷白钻石激光高光（`#ffffff` 加法混合），立体感极强；
  - **双层同心高斯光晕系统 (Dual Concentric Gaussian Halos)**：
    - 内层核心高亮光晕（`1.9x`，高亮度聚焦）；
    - 外层大气漫散光晕（`3.6x`，宽广柔和霓虹光池）；
  - **色彩感知平衡补偿**：针对蓝色（正常）、金色（预警）和红宝石色（风险）做了人眼发光度动态增益，悬停时均具备同等璀璨耀眼的强光辐射。
  - **呼吸脉动与遥测绑定**：方块内核“呼大吸小”自然呼吸，鼠标悬停时实时弹出详细遥测指标 HUD 浮窗。

---

## 2. 双大屏与视线交互系统

- **项目状态大屏 (Project Status Screen)**：
  - 超宽 129°（`2.25 rad`）环形曲面巨幕，默认位于红色方块聚集区远端（中心偏左，`statusRotY = -0.38π`），正面直接迎向摄像机；
  - **点击“项目状态看板”按钮视角**：镜头明显抬升至俯瞰高度（`camH: 5.48, lookY: 2.72`），大屏在上半部分左右横向撑满（占视口 88%~90%），水晶矩阵在下半部分一直向下延展铺满直抵屏幕底边（**完全零多余留白**），上下皆充实饱满。
- **敏捷会议大屏 (Meeting Screen)**：
  - 位于中心偏右（`leftRotY = +0.94π`），与项目状态大屏并排环绕；
  - **智能视线防遮挡 (Sightline Occlusion Ghosting)**：当旋转镜头导致会议屏遮挡主要观察视线时，大屏自动幽灵化半透明渐隐（`opacity: 0.20`），视线离开后平滑复原。

---

## 3. 操控手感与物理学算法

- **多通道统一点击引擎（Multi-Channel Click System）**：
  - 同时接入 `touchend`、`click` 与 `mouseup` 三重事件监听，配备 250ms 防重触发拦截，彻底保障所有移动设备（iOS Safari / Android Chrome / 微信内置浏览器）100% 能够捕获用户轻触；
  - **移动端特写镜头聚焦**：手机竖屏点击水晶时，镜头直接推进特写（`camDist: 6.4`，视线锁定水晶），所选方块高亮放大、其余方块淡化，产生极其强烈的视觉对焦反馈。
- **水晶对焦与双重取消机制（移动端/PC 全适配）**：
  - **再次点击已聚焦的水晶**：立即解除对焦（Toggle Deselect），自动恢复全景概览视角与大盘数据；
  - **点击水晶以外的任意空白/舞台区域**：立即取消对焦，镜头平滑回位。
- **大屏切线投影轨道拖拽算法**：
  - 基于鼠标/触控击中点轨道切线向量 $\vec{T} = (z, 0, -x)$ 与视口水平轴 $\vec{R}_{\text{cam}}$ 的点积投影；
  - 无论镜头处于正面、侧面还是俯仰视角，**手势向右划大屏必定顺滑向右转，向左划必定顺滑向左转**；
  - **松开即停**：移除多余的阻尼惯性漂移，按住移动，松手瞬间停稳。
- **全局控制**：
  - **左键/单指**：平移全景画面（未点中大屏时）或拖拽大屏（点中大屏时）；
  - **右键/双指**：360° 旋转全景视角；
  - **滚轮/双指捏合**：平滑推进/拉远变焦；
  - **顶部全屏按钮**：左上角一键切换全屏/退出全屏。

---

## 4. 移动端深度缓冲与抗闪烁优化

- **开启对数深度缓冲 (Logarithmic Depth Buffer)**：开启 `logarithmicDepthBuffer: true`，在全视锥体内提供均匀精度的对数深度分配；
- **优化摄像机近裁切面**：将 `near` 从 `0.1` 提升至 `0.8`，将移动端 GPU（Mali/Adreno）的深度分辨率提升 10 倍以上；
- **消除共面冲突 (Polygon Offset & Z-Fighting Elimination)**：
  - 水晶外壳开启 `polygonOffset: true, polygonOffsetFactor: 1.0`，彻底消除底棱与玻璃外壳的穿插闪烁；
  - 舞台边缘各装饰层拉开微米级物理高差，关闭发光装饰环与光晕平面的深度写入（`depthWrite: false`），消除边缘闪烁。

---

## 5. 性能瓶颈与高帧率极致优化

- **GPU 纹理上传带宽瓶颈根治 (188 MB/s $\to$ 0 MB/s 静态驻留)**：
  - **原致命问题**：原本在 `animate()` 循环中每 2 帧无条件调用一次 `updateStatusCanvas()` 并触发 `statusTexture.needsUpdate = true`，导致 2048x768（6.29MB 显存位图）以 30fps 的频率连续重新向 GPU 上传，形成每秒高达 188MB 的显存总线吞吐狂飙，直接造成中低端显卡、核显与移动端主线程卡死/掉帧；
  - **优化方案**：改为**状态变更事件驱动模式**，仅在用户鼠标悬停目标切换、点击方块或取消聚焦时单次触发 1ms 静态渲染与 GPU 绑定，闲置时 GPU 纹理上传带宽为 **0 MB/s**。
- **动画主循环零临时对象分配 (Zero-Allocation 60fps Pipeline)**：
  - 移除方块呼吸计算中每帧 52 次 `THREE.Color.clone()` 实例化，改用预分配静态工作变量 `_tempColorA` / `_tempColorB`；
  - 预缓存 `staticCubeOuterMeshes`，彻底消除 60fps 帧内垃圾回收（GC Spike）导致的微卡顿；
- **光线投射按需触发 (Demand-Driven Raycasting)**：
  - 仅在鼠标坐标真正发生位移时执行 3D 碰撞拾取，静止时跳过光线运算，CPU 占用率直降至 1%~2%。

---

## 6. 近期重要优化与调优记录

| 模块 | 优化事项 | 核心改动说明 |
| :--- | :--- | :--- |
| **性能调优** | **根治浏览器卡死与超高显存吞吐** | 消除 30fps 无条件 `statusTexture.needsUpdate`，GPU 上传吞吐从 188MB/s 直降为 0；移除 60fps 内 `Color.clone()` 垃圾回收压力 |
| **手机触控** | 手机端触控大屏轨道拖拽修复 | 引入 `{ passive: false }` + `e.preventDefault()` 阻止原生滚动；`recursive: true` 穿透识别大屏边框与滑轨部件 |
| **版本管理** | 页面右下角时间戳版本徽标 | 右下角新增赛博毛玻璃徽标 `vYYYYMMDDHHmm`（UTC+8 北京时间精确到分，无分隔符） |
| **PWA离线** | 手机端 PWA 离线可安装应用 | 自动化构建 manifest.json、Service Worker、高分辨率多端图标与 iOS 安装引导 |
| **手机触控** | 多通道统一点击引擎与移动端特写镜头 | 接入 touchend + click + mouseup 三通道原生兜底；手机竖屏机位直降推进（`camDist: 6.4`）实现明显特写聚焦 |
| **看板视角** | 点击“项目状态看板”按钮视角大幅下移贴底 | 明显提升机位并下压视线，水晶矩阵直接铺满下半区直抵屏幕最底边（完全 0% 底部留白） |
| **移动抗闪烁** | 手机端底棱与舞台边缘闪烁消除 | 开启对数深度缓冲 + near 调整至 0.8 + 水晶多边形深度偏移 + 装饰层禁用 depthWrite |
| **大屏拖拽** | 移除释放后的惯性漂移 | 鼠标/触控释放按键瞬间重置速度，实现按住拖动、松开即停 |
| **视觉光影** | 水晶棱镜方块光影质感升级 | 引入双层冷白激光倒角高光与双层 256x256 指数高斯漫散光晕 |
